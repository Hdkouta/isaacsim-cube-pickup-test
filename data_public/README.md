# Public Data Snapshot

This folder contains uploaded runtime data so it can be viewed outside the local Isaac Sim machine.

Included folders:

- teacher_data
- shadowhand_action_dataset
- pi0_action_eval
- pi0_action_eval_merged
- camera

Notes:

- These files are copied from C:\VScode\Yoshida_script.
- Script/code files are in the repository root folders (`eval`, `data_tools`, `bridge`, `run`, etc.).
- This snapshot is intended for browsing and analysis on GitHub.
